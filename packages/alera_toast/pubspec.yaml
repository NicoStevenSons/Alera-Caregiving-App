name: alera_toast
description: Native Android toast feedback for Alera notification actions.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.alera.alera_toast
        pluginClass: AleraToastPlugin
